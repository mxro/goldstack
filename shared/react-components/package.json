{
  "name": "@mxro/react-components",
  "version": "1.0.0",
  "source": "src/reactComponents.ts",
  "main": "dist/reactComponents.js",
  "types": "dist/",
  "files": [
    "dist"
  ],
  "bundledDependencies": [],
  "scripts": {
    "build": "npm run clean && npm run compile",
    "build:watch": "npm run clean && npm run compile:watch",
    "compile": "../../node_modules/.bin/microbundle build --jsx React.createElement",
    "compile:watch": "../../node_modules/.bin/microbundle watch --jsx React.createElement",
    "clean": "rm -rf ./dist",
    "prepublishOnly": "npm run build"
  },
  "author": "Max Rohde",
  "license": "MIT",
  "devDependencies": {
    "@types/react": "^16.9.35",
    "@types/react-dom": "^16.9.8"
  },
  "dependencies": {
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  }
}