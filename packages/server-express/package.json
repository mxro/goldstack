{
  "name": "@mxro/server-express",
  "version": "1.0.0",
  "description": "Sample express server",
  "main": "dist/serverExpress.js",
  "author": "Max Rohde",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && yarn compile",
    "build:watch": "yarn clean && yarn compile:watch",
    "compile": "tsc -p tsconfig.json",
    "compile:watch": "tsc -p tsconfig.json --watch",
    "test": "jest --passWithNoTests --config=jest.config.js",
    "clean": "rm -rf ./dist",
    "start": "yarn node dist/serverExpress.js",
    "start:watch": "nodemon --watch ../../ --ext js --exec 'yarn node dist/serverExpress.js'"
  },
  "dependencies": {
    "@mxro/lib-hello": "1.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.6",
    "@types/jest": "^25.2.3",
    "@types/node": "^14.0.6",
    "express": "^4.17.1",
    "jest": "^26.0.1",
    "nodemon": "^2.0.4",
    "ts-jest": "^26.1.0",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.3"
  }
}
